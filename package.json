{
  "name": "functional-acl",
  "version": "0.0.1",
  "description": "Implementation of ACL with functional aspects to identify subjects (users) and operations.",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha --compilers js:babel/register",
    "build": "babel -d lib/ src/",
    "prepublish": "npm run build",
    "coverage": "istanbul cover _mocha -- --compilers js:babel/register"
  },
  "author": "Paul 'opatut' Bienkowski <npm@opatut.de>",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.8.23",
    "chai": "^3.2.0",
    "istanbul": "^0.3.19",
    "higher-order-functions": "^0.0.1"
  }
}
