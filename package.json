{
  "name": "functional-acl",
  "version": "0.1.0",
  "description": "A functional implementation of access control lists (ACLs)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --out-dir . --stage 0 src/",
    "watch": "npm run build -- --watch",
    "prepublish": "npm run build",
    "example:simple": "babel-node --stage 0 examples/index.js",
    "example:express": "babel-node --stage 0 examples/express.js"
  },
  "author": "Paul 'opatut' Bienkowski <npm@opatut.de>",
  "license": "MIT",
  "devDependencies": {
    "babel": "^6.0.15",
    "express": "^4.13.3"
  }
}
